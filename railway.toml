[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
# First boot runs db:migrate; allow more time before /health is up.
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
